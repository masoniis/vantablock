# Vantablock Shared (`vantablock-shared`)

The `vantablock-shared` crate is the foundational bridge between the `client` and `server` crates. It holds all common knowledge, ensuring the client and server remain perfectly synced on game rules.
